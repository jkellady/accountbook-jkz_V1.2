/**
 * PWA Components — Barrel Export
 *
 * Centralised exports for all Progressive Web App components.
 * Import from this barrel to keep imports clean:
 *
 * @example
 * ```tsx
 * import { PWABootstrap, OfflineIndicator, InstallPrompt } from '@/components/pwa'
 * ```
 */

export { InstallPrompt } from './InstallPrompt'
export { OfflineIndicator } from './OfflineIndicator'
export { PWABootstrap } from './PWABootstrap'
